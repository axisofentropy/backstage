name: Build and push Demo image
on:
  repository_dispatch:
    types: [release-published]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      NODE_OPTIONS: --max-old-space-size=4096

    strategy:
      fail-fast: false
      matrix:
        node-version: [16.x]

    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          path: backstage
          ref: v${{ github.event.client_payload.version }}

      - name: use node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: https://registry.npmjs.org/ # Needed for auth

      - name: yarn install
        uses: backstage/actions/yarn-install@v0.6.0
        with:
          cache-prefix: ${{ runner.os }}-v${{ matrix.node-version }}

      - name: Use Uffizzi's backstage app config
        run: |
          cp -f ./.github/uffizzi/uffizzi.demo.app-config.yaml ./app-config.yaml

      - name: typescript build
        run: |
          yarn tsc

      - name: backstage build
        run: |
          yarn workspace example-backend build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: packages/backend/Dockerfile
          push: true
          tags: |
            registry.uffizzi.com/${{ github.repository_owner }}/backstage:demo
            registry.uffizzi.com/${{ github.repository_owner }}/backstage:demo-${{ github.event.client_payload.version }}
          labels: |
            org.opencontainers.image.description=Demonstration image generated from the latest Backstage release; This includes some additional catalogs.
